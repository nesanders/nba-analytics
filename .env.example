# Backend (Cloud Run env vars — set these in Cloud Run config, not committed)
GCS_BUCKET_NAME=nba-analytics-data-2026
ALLOWED_ORIGIN=https://nesanders.github.io

# Local dev only — never deployed
GROQ_API_KEY=gsk_...
